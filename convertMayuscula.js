function convertMayuscula(cadena){
    return cadena.toUpperCase();
}
console.log(convertMayuscula("hola caracola"));

const convertirMayuscula=(cadena1)=>{
    return cadena1.toUpperCase();

} 
console.log(convertirMayuscula("así es en flecha la función"));